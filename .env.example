# =============================================================
# ALTCORP WALLET - Template de ambiente
# Copie para .env e preencha os valores reais
#
# DEV:  docker compose --profile dev up --build
# VM:   docker compose --profile vm up --build -d
# PROD: docker compose --profile prod up --build -d
# =============================================================

# ---- Banco de dados ----
POSTGRES_USER=walletuser
POSTGRES_PASSWORD=TROQUE_POR_UMA_SENHA_FORTE
POSTGRES_DB=altcorp_wallet

# ---- Backend ----
# Gere com: python -c "import secrets; print(secrets.token_urlsafe(48))"
SECRET_KEY=TROQUE_POR_UMA_CHAVE_SECRETA_SEGURA
DEBUG=false
API_PORT=8000
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ---- Frontend ----
VITE_API_URL=/api/v1
VITE_GOOGLE_CLIENT_ID=

# ---- Google OAuth (Backend redirect flow - produção/domínio) ----
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://wallet.altcorphub.com/api/v1/auth/google-callback
FRONTEND_URL=https://wallet.altcorphub.com

# ---- VM/LAN (usado apenas no profile vm) ----
GOOGLE_REDIRECT_URI_VM=http://192.168.15.5:8000/api/v1/auth/google-callback
FRONTEND_URL_VM=http://192.168.15.5:8080

# ---- CORS ----
# Dev: incluir IP local. Prod: só o domínio.
ALLOWED_ORIGINS=http://localhost:8080,http://192.168.15.5:8080,https://wallet.altcorphub.com

# ---- Domínio ----
DOMAIN=wallet.altcorphub.com
DOMAIN_VM=192.168.15.5

# ---- Email (Resend) ----
RESEND_API_KEY=
RESEND_FROM_EMAIL=AltCorp Wallet <noreply@altcorp.com.br>

# ---- Groq AI (gratuito) ----
# Obtenha em: https://console.groq.com/keys
# Tier gratuito: ~14.400 req/dia LLMs + Whisper ilimitado
GROQ_API_KEY=
