# Production environment variables
# This file is used when building for production (npm run build)
# In production, we use relative paths to work with Nginx Proxy Manager or reverse proxy
# The API should be accessible at https://yourdomain.com/api/v1
VITE_API_URL=/api/v1
